services:
  app:
    container_name: app
    build: 
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/app  # Mount source code to container
    command: ["deno", "run", "-A", "main.ts"]